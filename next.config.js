/** @type {import('next').NextConfig} */
const nextConfig = {
  // IMPORTANT: Ensure 'output: 'export'' is NOT here.
  // This allows Next.js to run in its default server-side rendering mode,
  // which is suitable for dynamic data from your Django API.
};

module.exports = nextConfig;
